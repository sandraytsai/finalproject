createGates = function() {
  gates = this.add.group();
  gates.enableBody = true;

  gate1 = gates.create(1590, 0, 'gate');
  gate1.body.immovable = true;

  gate2 = gates.create(3190, 0, 'gate');
  gate2.body.immovable = true;

  gate3 = gates.create(4790, 0, 'gate');
  gate3.body.immovable = true;

  gate4 = gates.create(6390, 0, 'gate');
  gate4.body.immovable = true;

  gate5 = gates.create(7990, 0, 'gate');
  gate5.body.immovable = true;

  gate6 = gates.create(9590, 0, 'gate');
  gate6.body.immovable = true;

  gate7 = gates.create(11190, 0, 'gate');
  gate7.body.immovable = true;

  gate8 = gates.create(12700, 0, 'gate');
  gate8.body.immovable = true;
}

